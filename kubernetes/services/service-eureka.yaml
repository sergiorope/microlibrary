apiVersion: v1
kind: Service
metadata:
  name: eureka
  labels:
    app: eureka
spec:
  selector:
    app: eureka  
  ports:
    - protocol: TCP
      port: 8252       
      targetPort: 8252 
  clusterIP: None
